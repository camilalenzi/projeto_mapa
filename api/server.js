const express = require('express');
const cors = require('cors');

const port = 3000;
const host = 'localhost';

const app = express();
app.use(cors());

app.get('/', (req, resp) => {


})
